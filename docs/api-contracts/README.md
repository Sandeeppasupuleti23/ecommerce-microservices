# API Contracts

The gateway reserves these public route prefixes:

| Capability | Route prefix |
| --- | --- |
| Orders | `/api/orders` |
| Inventory | `/api/inventory` |
| Payments | `/api/payments` |

Detailed request and response contracts should be added here as each controller is implemented.